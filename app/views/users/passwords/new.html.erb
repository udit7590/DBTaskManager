<div class="center-items">
  <h1 class="devise-title"><i class="bi bi-list-task ilarge middle"></i> DB Task Manager</h1>
  <h2 class="devise-heading">Forgot your password?</h2>
</div>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= render "users/shared/error_messages", resource: resource %>

  <div class="field">
    <%= f.label :email, class: 'input-label' %>
    <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: 'Email to receive reset password link', class: 'input-text' %>
  </div>

  <div class="actions">
    <%= f.submit "Send me reset password instructions", class: 'db-btn db-btn-primary db-btn-fullwidth db-btn-large' %>
  </div>
<% end %>

<%= render "users/shared/links" %>
